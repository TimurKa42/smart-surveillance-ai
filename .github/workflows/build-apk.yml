name: Build Android APK
on:
  workflow_dispatch:
  push:
    paths:
      - "mobile/**"
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential git unzip openjdk-17-jdk \
            python3-pip python3-dev \
            libffi-dev libssl-dev \
            zlib1g-dev libncurses5-dev libtinfo5 \
            autoconf automake libtool pkg-config \
            zip liblzma-dev libbz2-dev libsqlite3-dev \
            cmake ninja-build
      - name: Install Buildozer and Cython
        run: pip install buildozer==1.5.0 cython==0.29.36 meson meson-python ninja
      - name: Set JAVA_HOME to 17
        run: echo "JAVA_HOME=$JAVA_HOME_17_X64" >> $GITHUB_ENV
      - name: Cache buildozer packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer/android/packages
            mobile/.buildozer/android/platform/build-*/packages
          # Ключ помечен -ndk27, чтобы не подхватить старый кэш,
          # собранный под NDK 25c со старым пином p4a — он несовместим
          # с новым NDK 27b и текущим (закреплённым) recipe numpy.
          key: buildozer-packages-ndk27-${{ hashFiles('mobile/buildozer.spec') }}
          restore-keys: |
            buildozer-packages-ndk27-
      # download.savannah.gnu.org периодически отдаёт 502 на freetype tar.gz
      # (это редиректор на "ближайшее зеркало" — сам редирект и шалит).
      # Кладём файл заранее ИМЕННО туда, где его ищет python-for-android:
      # $GITHUB_WORKSPACE/mobile/.buildozer/android/platform/build-arm64-v8a/packages/freetype/
      # (не ~/.buildozer/... — там p4a пакеты не ищет), и берём его с
      # download-mirror.savannah.gnu.org — прямого хоста без редирект-прослойки.
      #
      # ВАЖНО: версия freetype (2.14.1) должна совпадать с той, что просит
      # текущий закреплённый p4a.commit в buildozer.spec. Если обновляешь
      # p4a.commit — проверь лог сборки на строку "Downloading freetype from ..."
      # и, если версия там другая, поправь её и здесь.
      - name: Pre-download freetype (works around savannah 502s)
        run: |
          set -e
          URL="https://download-mirror.savannah.gnu.org/releases/freetype/freetype-2.14.1.tar.gz"
          DEST_DIR="$GITHUB_WORKSPACE/mobile/.buildozer/android/platform/build-arm64-v8a/packages/freetype"
          DEST_FILE="$DEST_DIR/freetype-2.14.1.tar.gz"
          MARK_FILE="$DEST_DIR/.mark-freetype-2.14.1.tar.gz"
          mkdir -p "$DEST_DIR"
          if [ -s "$DEST_FILE" ] && [ -f "$MARK_FILE" ]; then
            echo "freetype already present (from cache), skipping download."
            exit 0
          fi
          for i in $(seq 1 10); do
            echo "Attempt $i: downloading freetype..."
            if curl -fL --retry 3 --retry-delay 5 -o "$DEST_FILE" "$URL"; then
              echo "Downloaded successfully."
              touch "$MARK_FILE"
              break
            fi
            echo "Failed, waiting before retry..."
            rm -f "$DEST_FILE"
            sleep 20
          done
          if [ ! -s "$DEST_FILE" ]; then
            echo "Could not download freetype after multiple attempts, continuing anyway (buildozer will try itself)."
          fi
      - name: Build APK
        env:
          PIP_NO_BINARY: fonttools
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 45
          max_attempts: 5
          retry_wait_seconds: 90
          command: cd mobile && yes | buildozer -v android debug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: smart-surveillance-apk
          path: mobile/bin/*.apk
